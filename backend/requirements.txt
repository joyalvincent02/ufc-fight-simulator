# ============================================================
# MMA Math Backend - Production Requirements
# ============================================================
# Deployment target: Azure App Service (Linux, Python 3.12)
#
# Key notes:
#   - xgboost-cpu replaces xgboost to avoid the nvidia-nccl-cu12
#     transitive dependency (~190 MB GPU library, useless on App Service).
#   - playwright is required in production: ufc_scraper.py launches
#     a Chromium subprocess to scrape ufcstats.com.
#     Run `playwright install chromium` after pip install
#     (handled in startup.sh and the GitHub Actions workflow).
#   - aiohttp and curl_cffi have been removed - neither is actually
#     called anywhere in the production code paths.
# ============================================================

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
gunicorn>=22,<24
python-multipart>=0.0.9,<1.0

# --- Database ---
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0,<2.0

# --- HTTP / scraping ---
requests>=2.32,<3.0
playwright>=1.47,<2.0
beautifulsoup4>=4.12,<5.0

# --- Scheduling ---
apscheduler>=3.10,<4.0

# --- Data / ML ---
numpy>=1.26,<3.0
pandas>=2.0,<3.0
joblib>=1.3,<2.0
# NOTE: scikit-learn pickle files are version-sensitive.
# The model is retrained post-deploy via /retrain-ml-model,
# so the training and inference versions will always match.
scikit-learn>=1.5,<2.0
# CPU-only variant: no CUDA, no nvidia-nccl-cu12 dependency.
# Saves ~190 MB on install and eliminates GPU library conflicts.
xgboost-cpu>=2.1,<4.0
